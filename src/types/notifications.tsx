export interface NotificationType {
    id?: number,
    title?: string,
    body?: string,
    notification_type?: string,
    created_time?: string,
    provider_id?: number,
    provider_name?: string,
    seen?: boolean,
}
